import { CustomParagraphDirective } from './custom-paragraph.directive';

describe('CustomParagraphDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomParagraphDirective();
    expect(directive).toBeTruthy();
  });
});
